"""
Test suite for the modems package. Kept as a regular package (rather than relying
on pytest's implicit rootdir/namespace-package import) so that cross-module helper
imports are unambiguous regardless of how pytest is invoked.
"""
